export function isAnagram(s: string, t: string): boolean {
	if (s.length!==t.length)return false

	// const set =new Set().add([...s])
	// set.add([...t])
	//
	//
	// console.log(set)
	return false
};
